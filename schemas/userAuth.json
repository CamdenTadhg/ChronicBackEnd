{
    "type": "object", 
    "properties": {
        "email": {
            "type": "string",
            "pattern": "^[A-Za-z0-9._%+-]+@[a-zA-Z0-9.-]+\\.[a-zA-Z]{2,}$"
        },
        "password": {
            "type": "string"
        }
    },
    "additionalProperties": false,
    "required": [
        "email",
        "password"
    ]
}